# Data Directory

Generated analysis datasets are intentionally not committed. Recreate them from
the scrape archive and the scripts in `scripts/`.

The only committed data file is `kollam-lifecycle-kaplanmeier.csv`, a small
curated ONCourts benchmark input used by the Kerala comparison scripts.
